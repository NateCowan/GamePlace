
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
        }
        .profile-card {
            max-width: 600px;
            margin: 50px auto;
            background-color: #fff;
            border-radius: 12px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            padding: 40px;
        }
        .profile-picture {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            overflow: hidden;
            margin: 0 auto 20px;
            border: 5px solid #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .profile-picture img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .profile-info {
            text-align: center;
        }
        .profile-info h2 {
            margin-bottom: 10px;
            color: #333;
        }
        .profile-info p {
            color: #666;
            margin-bottom: 5px;
        }
        .profile-info a {
            color: #007bff;
            text-decoration: none;
        }
        .profile-info a:hover {
            text-decoration: underline;
        }
    </style>

</head>
<body>
    {{>nav}}
  
    <div class="container">
        <div class="profile-card">
            <div class="profile-picture">
                <img src="https://ui-avatars.com/api/?name={{firstInitial}}&background=random&color=fff&length=1" alt="Profile Picture">
            </div>
            <div class="profile-info"> 
                {{!-- Displays username --}}
                <h2>{{username}}</h2>
                
            </div>
        </div>
    </div>

    <h1>Followed Games:</h1>
    {{#if followedGames.length}}
    {{#each followedGames}}
        <div class="card">
        <div class="card-body">
            <h5 class="card-title">{{this.game_title}}</h5>
            <!-- Add more game details if available -->
            <form action="/game" method="GET">
                <input type="hidden" name="game_title" value="{{this.game_title}}">

                <input type="submit" value="Take a Look">

            </form>
        </div>
        </div>
    {{/each}}
    {{else}}
    <p>You are not following any games.</p>
    {{/if}}




</body>
</html>
